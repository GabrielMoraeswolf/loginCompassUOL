import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    arrowButton: {
        padding: 5,
        width: 30,
        resizeMode: 'contain',
    },
  });

  export default styles