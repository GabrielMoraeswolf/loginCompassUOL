import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    arrowButton: {
        padding: 5,
        width: 28,
        resizeMode: 'contain',
    },
  });

  export default styles