/*import { StyleSheet } from "react-native";
import { Colors } from "../styles";

const styles = StyleSheet.create({
    tabBarIcon: {
        width: 20,
        height: 20,
    },
    tabBarLabel: {
        fontSize: 12,
       // fontWeight: 'bold',
        color: Colors.cardTitle,
    },
    tabBarLabelFocused: {
        color: Colors.primary, // Defina a cor desejada para o texto quando estiver pressionado
    },
  });

  export default styles*/